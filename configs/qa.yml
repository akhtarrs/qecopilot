# QA Environment Configuration
# BASE_URL and API_URL come from .env — set them to your QA URLs there.
environment:
  name: qa
  base_url: "${env.BASE_URL}"
  api_url: "${env.API_URL}"
  flags:
    debug_logging: true
    mock_payments: true
    mock_email: true
    mock_sms: true
